<?php
/*
if(isset($tipo))
{
	if($tipo=="vid") // show a video
	{
		echo "<div id=\"vid_$id_MED\" class=\"videodiv\"></div>";
		echo "<script type=\"text/javascript\">$.doVID('vid_$id_MED','".VIDEOS_AS_URL.$url."')</script>";
	}
	elseif($tipo=="img") // show an image
	{
		if($link!="") // the image as a link
			$link = "http://$link";
		if(START_ID_GALL_LINK&&START_RAND_GAL)
			if(FLATLINK)
				$link = "/$cmslan/".PORT_SYMLINK."/".$arr_start_id_GALL[1];		
			else
				$link = "?p=".PORT_ID."&pp=$arr_start_id_GALL[0]&lan=$cmslan";
		if($link!="")
			echo "<a href=\"$link\">";
		echo "<img src=\"".IMAGES_AS_URL."/$url\" />";
		if($link!="")
			echo "</a>";
	}
}
*/
?>